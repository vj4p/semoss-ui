# Dev Variables
# path to the monolith server
ENDPOINT=http://localhost:9090

# name of the deployed instance (proxied at this prefix in vite.config.ts)
MODULE=/Monolith

# To sign in to a local Monolith with an access key, put ACCESS_KEY and
# SECRET_KEY in .env.local (gitignored). A production build never reads them.
